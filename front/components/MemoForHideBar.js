// listItesm to be memoed.

//only for local state.
// redux state to be managed higher level, and do not pass on to too small components.

//if it is just true, false state, keep them inside, managing with callback function but with useCallback.

// to change folder structure before it is more complicated.

// container-higher components with redux status(disatch)
//components-sub components with local state(but useSelect possible.) Also, Memoized components.
